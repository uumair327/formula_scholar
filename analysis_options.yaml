# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# Golden Rule 14: Agent-Friendly Code — strict linting catches issues before
# they reach code review or production.

include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # ── Code Quality ──
    prefer_single_quotes: true
    always_declare_return_types: true
    cancel_subscriptions: true
    close_sinks: true
    require_trailing_commas: true
    avoid_empty_else: true
    avoid_relative_lib_imports: true

    # ── Const & Immutability ──
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_fields: true

    # ── Safety ──
    unawaited_futures: true
    avoid_print: true
    avoid_type_to_string: true
    no_duplicate_case_values: true
    prefer_void_to_null: true
    unnecessary_await_in_return: true

    # ── Style Consistency (Golden Rule 16) ──
    sort_constructors_first: true
    unnecessary_this: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    unnecessary_string_interpolations: true
    use_string_buffers: true
    unnecessary_null_checks: true
    use_super_parameters: true

    # ── Documentation ──
    slash_for_doc_comments: true

analyzer:
  # Server-side scripts use dart_firebase_admin which isn't a Flutter
  # dependency. They are standalone Dart scripts run separately.
  exclude:
    - bin/**
    - scripts/**
  errors:
    # Treat these as errors to enforce discipline:
    unawaited_futures: warning
    cancel_subscriptions: error
    close_sinks: error
    avoid_print: warning
