{
  "name": "formula-factory-migration-cli",
  "version": "0.1.0",
  "description": "Migration CLI for Formula Factory: discover, stage, validate, promote, rollback",
  "main": "index.js",
  "bin": {
    "migration-cli": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "csv-writer": "^1.6.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
